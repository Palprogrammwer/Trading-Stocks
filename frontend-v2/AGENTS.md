# AGENTS

Diese Version ist bewusst getrennt von der alten App. Aendere fuer V2 nur Dateien in `frontend-v2/`, sofern nicht explizit anders gewuenscht.

Die Vercel API-Routen liegen in `frontend-v2/api/`. Gemeinsame Logik gehoert nach `frontend-v2/lib/`.
